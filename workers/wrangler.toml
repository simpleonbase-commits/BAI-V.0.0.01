name = "bai-background-check"
main = "bai-background-check.js"
compatibility_date = "2024-01-01"

# Configure your route (uncomment and update with your domain)
# routes = [
#   { pattern = "api.yourdomain.com/check/*", zone_name = "yourdomain.com" }
# ]

[vars]
# Non-sensitive environment variables go here

# Secret variables (set via wrangler secret put BASESCAN_API_KEY)
# BASESCAN_API_KEY = "your-api-key"
